/*Написать функцию, которая принимает количество секунд, переводит их в часы, минуты и секунды и возвращает в виде строки «чч:мм:сс».*/

let ss = +prompt("ss");

function hour(ss) {
  return;
}

alert(Math.floor(ss / 3600) + ": " + ss / 60 + ": " + (ss % 60));

/*poka ne dodelal*/
